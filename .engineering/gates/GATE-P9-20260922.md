# P9 Gate — 2026-09-22

结论：原路线四条出口 PASS。常规 `mvn -q clean verify` 151项、失败/错误/跳过均0；独立规模测试1项通过；重建 CLI 实际退出0（erp_it 租户280，snapshot30）。

1. ReportSnapshotIT 从真实采购/销售/收款核销重建五类报表，库存与台账一致。
2. 重建重复结果一致、分批游标恢复、并发步骤不重复、来源变动拒绝发布、租户隔离、取消/清理保护均实测。
3. ReportScaleBenchmark 在 PostgreSQL16.15 / Java21 本地环境，以200万库存桶和对应流水、50万不同SKU维度、采购与销售各8000单/50000行、AR/AP各8000单测量。每类30个样本，查询包括全量汇总+首200行+AR/AP账龄。P95：库存591.70ms、采购90.21ms、销售116.68ms、AR29.93ms、AP25.00ms。详见 REPORT_SCALE-P9.json。样本为测试库专用租户的合成规模数据，非线上容量承诺；已自动清理。
4. 报表 Mapper 只含 rpt_*；源事实 SQL 留在各所有者模块，架构测试通过。

限制：显式刷新快照，非实时看板；持续变更期间可能 STALE，仍提供上一已发布快照。各类独立快照，不宣称统一结账时点。无合同到期日，因此是未核销账龄而非逾期账龄。没有新增生产HTTP或OIDC。

下一阶段：P10 可观测性，再 P11 本地运行与 CI。
