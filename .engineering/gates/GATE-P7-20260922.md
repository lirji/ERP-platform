# P7 Gate — 2026-09-22

结论：P7 内部应用服务出口 PASS；生产 OIDC/HTTP 与历史账回填仍不属于本次已验证能力。146 项全量测试，无失败/错误/跳过，详见 TEST_RESULT-P7.json。

| 原路线出口 | 实测证据 |
|---|---|
| 采购退货减库存并冲减应付 | ReturnE2EIT 已付采购退货：库存 10→6、红字 -40、供应商待退款 40；原付款 100 保留 |
| 销售退货显式批次并冲减应收 | ReturnE2EIT 拒绝空批次；原批次仍 0，RETURNED 批次回库 3/成本21；分三次红字累计1，无舍入遗失 |
| 调拨公司数量含在途守恒 | StockOperationIT：调出源6+在途4+目标0，接收源6+在途0+目标4；重复/并发仅一次，目标冻结失败保持在途 |
| 盘点差异审批后入账 | StockOperationIT：冻结桶拒绝业务及旧 SQL 出库，未经审批不可过账；取消解冻；差异执行后账/流水一致 |
| 移动加权均价 | InventoryCostIT：10/100+20/400，出3成本50，余额450；并发入库、清仓尾差、未知历史与旧版写入保护 |

新增退货来源上界及并发、红字/退款额度并发、现金/核销事实保留、失败回滚、租户隔离、幂等内容冲突、零价退货均有实际 PostgreSQL 验证。

边界：单桶调拨整单收发，同公司同批次；单行退货独立单据；退款为人工确认账务事实，无银行网关。历史来源金额或成本不可猜算，缺少来源金额拒绝退货；未知成本显式 NULL。库存成本按库存桶移动加权。报表/可观测性/本地全链路仍须继续 P9–P11。

P8：原条件未触发，当前不存在会签/并行审批需求；沿用内置顺序审批，不引入 workflow-platform/MQ。
