# Gate Report — P2 Master Data

```text
Phase:               P2 Master Data
GateId:              GATE-P2-20260921
Gate Status:         PASS_WITH_ASSUMPTIONS
Transition Decision: AUTO_CONTINUE → P3 Inventory Core
```

## 1. 出口条件逐项判定（ROADMAP P2）

| # | 条件 | 结果 | 证据 |
|---|---|---|---|
| ① | 编码在租户内唯一（并发建档只成功一条） | **PASS** | `并发建档只成功一条`：50 线程栅栏并发同一编码，恰好 1 成功，库中 1 行；`跨租户同码互不影响` |
| ② | 停用的主数据不能被**新**单据引用，已有单据不受影响 | **PASS** | `停用只切断未来引用`：停用后新引用报 `ERP-MD-3001`，已有引用登记与快照均不变；`停用单位不可被新SKU引用` |
| ③ | 修改 SKU 名称后，历史单据显示的仍是快照值 | **PASS** | `历史单据显示快照值`：改名后实时值变、已持有快照不变、新引用取到新值 |
| ④ | 关键字段（编码、基本单位）被引用后修改被拒 | **PASS** | `被引用后关键字段锁定` 报 `ERP-MD-3002` 且 `lockedFields` 含 `baseUnitId`；`未被引用可改关键字段`、`被引用后描述字段仍可改` 构成对照 |
| ⑤ | 批量导入 1000 行，校验失败整批回滚 | **PASS** | `批量导入成功`（1000 行全入）、`批量导入整批回滚`（第 37 行非法 → 0 行写入）、`批量导入返回全部失败行` |

## 2. 测试

```text
mvn clean verify → EXIT 0（连续两次，证明可重复执行）
单元 / 领域 / 架构：29
集成（真实 PostgreSQL）：40   （P1 基线 27 → 新增 13）
合计：69
```

## 3. 数据库规范

12 张 `md_*` 表、107 条注释。实测 `md_*` 无注释字段数 = **0**，
由 `MigrationCommentConventionTest` 在构建期强制（含负向证明）。

## 4. Assumptions

| 项 | 分类 | Carry Forward |
|---|---|---|
| **CAP-G02（MinIO 附件）未实现** | `DECISION_DEFERRED` —— P2 五条出口条件均不涉及附件；无真实场景即接对象存储属于提前建设 | 是 |
| `md_warehouse_location` 建表但 MVP 不启用（`A-11`） | `NON_BLOCKING` —— 维度先建好，避免日后改台账主键 | 是 |
| 供应商/客户/仓库沿用 SKU 的同一套规则，但仅 SKU 有完整用例 | `NON_BLOCKING` —— 规则在 §1 统一定义，同构实现随引用方阶段补齐 | 是 |
| `RISK-VER-01`、`Q-09`、`SPECIFIED_ORG/COMPANY` | 沿用 P1 判定 | 是 |

## 5. Blocking Issues

**对进入 P3：无。**

**对生产发布：`BLOCK-P1-01` 仍然开启** —— 生产环境无认证路径，OIDC 未接入。
在其关闭前系统不得对外暴露。

## 6. Next Phase

```text
P3 Inventory Core（CAP-C01..C04、CAP-S05）—— 本项目最关键的一期
```

出口条件中不可妥协的三条：
- **INV-01**：随机 1000 次过账后，余额 == 流水代数和；
- **INV-02**：并发 50 线程扣同一桶，无负库存、无超卖；
- **INV-04**：同一来源行过账 10 次只有一次效果。

且要求 `available` **不得有对应数据库字段**（由代码审查 + 架构测试保证）。
