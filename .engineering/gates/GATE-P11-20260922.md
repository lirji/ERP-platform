# P11 本地交付验收

状态：PASS（本地与远程 CI）。仅覆盖已批准的本地内部服务 MVP。

| 出口 | 证据 |
|---|---|
| Compose 5 分钟内健康 | 既有本地 PostgreSQL/镜像，应用重建启动 7.440 秒；最终镜像重新构建启动通过；CI 在空库验证冷启动 |
| 数据重复/清理/验证 | init→init→verify→cleanup→cleanup→init→verify 全部通过；只操作标记租户 990001 |
| Mock 入库 | DemoDataSeed 写实际 PostgreSQL 并调用真实服务；扫描无 .tsx/.jsx/.vue/.html 前端源码，前端项 N/A |
| P2P/O2C E2E | 本地 mvn clean verify 158 项，失败/错误/跳过均 0；远程结果见下 |
| 无无用中间件 | Compose 仅 app 与 PostgreSQL，实际业务及健康检查使用 |

原计划员工链路缺口已补：入职、绑定用户、离职禁用和保留历史，真实数据库验证。生产认证和业务 HTTP 仍是历史未完成项。

首次 CI 35681078016 发现 V2 重命名依赖 V30 导致空库失败。新增 B30 累计基线，仅空库使用；历史迁移字节/校验值保持不变，不 repair 历史。FreshSchemaMigrationIT 每次新建随机隔离 schema，完整迁移并对比已有库表列、验证重复迁移，清理仅作用于自身 schema。

代码复核：凭据不提交；容器只监听回环；非 root/只读应用文件系统；种子 ID/租户标记冲突拒绝，业务命令幂等，清理按明确租户限定；PermissionEnforcementIT 全局删除改为自身租户，保护新增员工外键和其他测试数据。财务三处原有格式改动保留并排除交付。

边界：P8 条件未触发；OIDC 客户端、业务 HTTP/UI、历史 v1 金额回填未完成，未生产部署。报表规模实测与 P10 可观测性证据沿用对应 Gate，不把本地观测当生产 SLA。

远程验证：[run 35681340638](https://github.com/lirji/ERP-platform/actions/runs/35681340638)，绑定提交 `e735c068dd1bdc55ea2f76ef503a947725344d54`，所有步骤 SUCCESS。JUnit artifact 已下载核对；158 项回归及显式种子验证通过。最终 main 运行在交付记录中补充。
