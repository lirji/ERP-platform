# Gate Report — P6 AR / AP / Settlement

- 结论：**PASS_WITH_ASSUMPTIONS**，P6 七条阶段出口全部 PASS；MVP 两条后端内部业务闭环达到验收。
- 命令：`mvn -q clean verify`，2026-09-22 09:41，退出码 0。
- 结果：单元/架构 33 + 真实 PostgreSQL 集成 87 = **120**；失败/错误/跳过均 0。
- 源码指纹（按排序的 erp-*/src 文件路径、NUL、内容依次 SHA-256）：`0e0ed3f66a20cf0d55748a21e2e0ef980ac3ee1454c38241652f9631ed41f4f5`。
- 本地日志 `/tmp/erp-p6-verified.log`，报告 `erp-app/target/failsafe-reports/`；可持久化汇总见 TEST_RESULT-P6.json。

## 阶段出口

| # | 出口 | 实测证据 | 结果 |
|---|---|---|---|
| ① | 采购到付款闭环 | FinanceSettlementE2EIT.采购到付款核销完整闭环并追溯源头：采购→审批→收货入库→应付→付款→核销，两端余额一致 | PASS |
| ② | 订单到收款闭环 | 订单到收款核销闭环且信用随反核销恢复：订单→审批→预占→出库→发运→签收→应收→收款→核销；授信恢复可再下单 | PASS |
| ③ | INV-06 同一入库事件十次仅一张 AP | 同一入库事件投递十次仅一张应付；并发重复事件唯一且冲突内容拒绝 | PASS |
| ④ | INV-07 超额付款/重复核销/超额核销拒绝 | 超额付款重复核销与超额核销拒绝且两端回滚；并发付款与核销不超额且并发反核销一次 | PASS |
| ⑤ | 反核销恢复金额并保留反向记录 | 反核销生成负记录并恢复两端且可再核销（AR/AP 对称）；重复反核销拒绝 | PASS |
| ⑥ | 本地 Outbox P95 ≤60s | OutboxLatencyIT 启用实际后台轮询；20 条事件、间隔 1000ms、P95 **1.13772545s**，20 张 AP 全部落库 | PASS |
| ⑦ | 每张 AR/AP 追溯源订单 | 两条闭环分别断言 财务单→收发单→订单，收付款单也能追溯财务单 | PASS |

## 补充验证与复核修复

- 15 项 FinanceSettlementE2EIT + 1 项 OutboxLatencyIT 新增集成验收；P5 及之前阶段全部回归。
- 同键同内容收付款重试返回原单，同键不同内容拒绝；跨租户、串单、币种不一致、非正数和超精度拒绝。
- 两端回滚测试特意让往来单更新成功而现金端超额，确认原事务完整回滚。
- 真实消费者配置缺失失败回滚；恢复编号配置后重投成功；坏消息/关系投影失败不阻断同批好消息，失败状态确实进入重试。
- 多行销售舍入、分批舍入、并发分批收货累计金额与整单金额一致。
- 复核修复了应用层越过 domain 直接依赖 FinanceMapper 的架构违规，完整矩阵/六边形规则均通过，没有放宽测试。
- V90 五张表及每列均有中文 COMMENT；14 个迁移由 Flyway 验证，真实隔离数据库运行 PostgreSQL 16.15。
- 未新增依赖、中间件或可部署服务；后台调度是 app 装配层复用既有 Outbox。

## 限制及停止边界

1. **本次完成后端内部业务闭环**。没有新增销售/财务 HTTP Controller、前端或银行通道；不能声称生产业务系统已可对外使用。
2. 历史 BLOCK-P1-01（生产 OIDC 未接入）仍开放。未执行生产部署。
3. 历史 v1 事件缺金额/币种，不自动补账；新业务用完整 v2。存量迁移需要来源模块回填与对账，未运行的工作不标 PASS。
4. 币种/编号规则由业务租户显式配置；测试 fixture 写入隔离租户，不能据此假定生产配置齐备。
5. 业务日期仍沿用现有 Asia/Shanghai 单时区约定；多时区、税额、汇兑、银行退款不在本轮。
6. 延迟仅为本地 20 样本测量，不是生产容量或可用性承诺。
7. 没有发现原 Runtime run_id；本报告为本地实现与验证证据，不伪造 Runtime 自动执行状态。远程 CI 未作为本地 PASS 声称。

用户本轮明确终点为 P6 MVP，因此不自动扩展 P7–P11；Git 交付完成后结束本任务。
