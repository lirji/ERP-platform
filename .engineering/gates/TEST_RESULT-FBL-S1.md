# FBL-S1 验收

2026-09-23 PASS。后端185项、前端11项通过，0失败/错误/跳过；修订指纹见同名JSON。

- 真实DB/HTTP：普通用户403、受保护角色403、未知写字段/非法幂等键400、跨租户404、固定权限目录、旧角色数组响应兼容。
- 同键4路并发只创建一个角色；两路同版本修改只有一个200、另一个409；失败不残留成功命令；角色权限替换及版本原子提交。
- 成功重放返回原资源；内容冲突409；撤销管理员权限后重放403。配置审计记录前后值，与业务同事务；重放不重复审计。
- 真实Casdoor/JAR18500浏览器：模拟创建已提交但响应被丢弃，错误提示真实可见、表单保留、再次提交只有一角色；分配采购只读权限、停用、刷新/退出通过。
- 初始化脚本复用已有OIDC绑定用户，重复执行无重复授权；不存在身份拒绝。测试角色不绑定用户，浏览器失败运行残留的无权限角色已通过正式接口停用，保留审计。
- 管理按钮的稳定可访问名称、加载态及侧栏布局已视觉复核。浏览器脚本已修复搜索后等待渲染、收起权限下拉后保存的时序问题。

限制：S2范围配置、用户分配、完整授权锁及采购链尚未实施；Docker构建链沿S0，S1真实运行验收为JAR，不宣称新镜像或生产部署完成。S1不启用仓库授权，范围待配置如实显示。

SKILL_HANDOFF：implementation-validation COMPLETED/PASS；S1 DONE；next=task-git-delivery→S2A。组织树2000节点边界随实际提供该接口的S2A一起验证。
