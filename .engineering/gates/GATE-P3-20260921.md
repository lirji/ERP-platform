# Gate Report — P3 Inventory Core

```text
Phase:               P3 Inventory Core ★ 本项目最关键的一期
GateId:              GATE-P3-20260921
Gate Status:         PASS_WITH_ASSUMPTIONS
Transition Decision: AUTO_CONTINUE → P4 Procure to Receive / P5 Order to Ship（可并行）
```

## 1. 出口条件逐项判定（ROADMAP P3）

| # | 条件 | 结果 | 证据 |
|---|---|---|---|
| ① | **INV-01** 随机 1000 次过账后 余额 == 流水代数和 | **PASS** | `余额等于流水代数和`：固定种子 1000 次随机进出，>500 次真实过账（反空跑断言），余额 == 流水和 == 独立累计值 |
| ② | **INV-02** 并发 50 线程扣同一桶无负库存、无超卖 | **PASS** | `并发扣减不超卖`：100 库存、50 线程各扣 3，恰 33 成功、17 报 `ERP-INV-3001`，余 1，且并发后 INV-01 仍成立 |
| ③ | **INV-04** 同一来源行过账 10 次只有一次效果 | **PASS** | `重复过账幂等`（串行 10 次）+ `并发重复过账幂等`（20 线程），流水均只 1 条 |
| ④ | 预占→部分消耗→释放 数量链正确，释放不超额 | **PASS** | `预占消耗释放链`：100/预占30/消耗12/释放18，on_hand·reserved·available 三值逐步断言；`释放不得超额` 报 `ERP-INV-3002` |
| ⑤ | 每条流水都能反查到来源单据行 | **PASS** | `流水可反查来源`；`无来源单据拒绝过账` 报 `ERP-INV-1001` |
| ⑥ | `available` **无对应数据库字段** | **PASS** | `AvailableIsDerivedTest` 扫描全部迁移禁止该列（含负向证明）；数据库实测 `inv_balance` 匹配 `%avail%` 的列数 = 0 |
| ⑦ | 对账脚本可跑通 | **PASS** | `scripts/reconcile-inventory.sh` 实跑：`erp_it` 与 `erp` 两库均「对账通过」退出码 0；表缺失时退出码 1（失败可见） |

## 2. 测试

```text
mvn clean verify → EXIT 0
单元 / 领域 / 架构：31   （新增 AvailableIsDerivedTest + 负向证明）
集成（真实 PostgreSQL）：50   （P2 基线 40 → 新增 10）
合计：81
```

## 3. Real Repository Smoke

```text
docker compose 起库 → healthy
java -jar erp-app.jar → /actuator/health = UP（4s）
Flyway → now at version v60
./scripts/reconcile-inventory.sh → 对账通过（退出码 0）
```

## 4. 架构测试拦下的真实缺陷

`application` 层原本直接依赖 `infrastructure` 的 MyBatis Mapper，违反六边形分层，
`ModuleDependencyArchitectureTest` 报 **37 处违规**。已补 `domain/InventoryRepository`
端口 + `infrastructure` 适配器，`application` 只依赖端口
（依赖方向：`application → domain ← infrastructure`）。

> 这是架构测试本阶段的直接价值：它拦下的不是风格问题，而是会让库存内核
> 绑死持久化技术、且无法脱离数据库测试领域规则的结构错误。

## 5. Assumptions

| 项 | 分类 | Carry Forward |
|---|---|---|
| 库位 `location_id` 用哨兵 `0`（`A-11`） | `NON_BLOCKING` —— 维度已在主键，启用时只填值 | 是 |
| 计价（移动加权平均，CAP-S10）未实现 | `DECISION_DEFERRED` —— ROADMAP 置于 P7；流水不可变，任何计价方法都可重算 | 是 |
| 调拨在途 `in_transit` 建列未使用 | `DECISION_DEFERRED` —— 调拨在 P7（CAP-S03），INV-08 守恒届时验证 | 是 |
| 负库存策略读配置尚未接线（默认禁止） | `DECISION_DEFERRED` —— `Q-09` 未答复；当前由 CHECK 约束硬性禁止负库存 | 是 |
| `RISK-VER-01`、`BLOCK-P1-01` | 沿用既有判定 | 是 |

## 6. Blocking Issues

**对进入 P4 / P5：无。**

**对生产发布：`BLOCK-P1-01`（生产无认证路径）仍然开启。**

## 7. Next Phase

```text
P4 Procure to Receive  （CAP-C05、CAP-S09）
P5 Order to Ship       （CAP-C06、CAP-S06）
```

二者**都只依赖 P3 且互不依赖，可并行**（ROADMAP §2 依赖图）。
P4 需 `Q-09`（超收比例 / 负库存）答复；未答复时按「默认禁止、配置可放开」继续。
