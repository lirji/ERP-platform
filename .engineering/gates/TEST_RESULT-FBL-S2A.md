# FBL-S2A 验收

2026-09-23 PASS；源码/JAR指纹见同名JSON。

- 后端全量189项通过，前端12项、类型、构建、lint通过。
- 真实DB/SQL：按权限分别OR角色公司集合；广范围读不放大写；跨租户、无权限、无明细/停用角色拒绝；组织前缀不误匹配相邻兄弟。
- 范围保存同事务包含版本、明细、幂等和前后审计；精确ID回显；空指定集合、部门冒充公司、非法引用均400；重复请求无重复效果。
- 授权组织森林超过2000节点422，未截断伪装完整；旧/me保持字段/枚举摘要，指定范围不再降级放行旧查询。
- 真实Casdoor/JAR18500：创建丢响应重放、分配权限、选择真实公司、范围保存、关闭重开回显、停用与退出PASS，截图视觉复核通过。
- 修复必填控件可访问名称与表单ID隔离。验证期间新增测试的泛型类型错误曾阻断前端构建；已修复，用失败即停命令重建，逐文件比较JAR==dist后重新验收，不沿用旧包浏览器结果。

边界：当前仓库授权NONE；用户角色分配/业务命令授权修订为S2B；采购等接口按后续片绑定ActionScope。

SKILL_HANDOFF：implementation-validation COMPLETED/PASS；S2A DONE；next=task-git-delivery→S2B。
