# Gate Report — P1 Platform Kernel

```text
Phase:              P1 Platform Kernel
GateId:             GATE-P1-20260921
Gate Status:        PASS_WITH_ASSUMPTIONS
Transition Decision: AUTO_CONTINUE → P2 Master Data
```

## 1. 出口条件逐项判定（ROADMAP P1）

| # | 条件 | 结果 | 证据 |
|---|---|---|---|
| ① | OIDC 登录拿到 `AccessContext` | **PARTIAL** | `PermissionEnforcementIT.装配AccessContext` 通过：由身份装配出含权限点与数据范围的上下文，且经完整 HTTP 栈验证。**但身份来源是临时请求头通道（默认关闭），OIDC 令牌校验尚未接入** |
| ② | 无权限用户调 API 返回 403 | **PASS** | `PermissionEnforcementIT` 6/6：403 + `ERP-AUTH-4001`、401、停用用户 4004、用户不存在不泄漏 |
| ③ | `org_path` 数据权限生效，**且 SQL 中出现前缀条件** | **PASS** | `DataScopePushdownIT` 5/5：断言生成的 SQL 含 `org_path` 与租户条件；含兄弟部门前缀不误匹配用例 |
| ④ | 并发 50 线程取号无重复、无空洞 | **PASS** | `NumberGeneratorConcurrencyIT` 3/3：真实 PostgreSQL + 真实事务 + 50 线程栅栏并发 |
| ⑤ | 非法状态迁移被拒 + 并发迁移只有一个成功 | **PASS** | `StateMachineTest` 7/7（非法迁移）+ `DocumentStateConcurrencyIT` 3/3（50 线程恰好一个成功） |
| ⑥ | 审计含前值/后值/IP/traceId | **PASS** | `AuditTrailIT` 4/4：逐项断言谁/何时/改了什么/前值/后值/IP/终端/traceId + 脱敏 |
| ⑦ | Outbox 投递失败可重试并进 DLQ | **PASS** | `OutboxRetryAndDlqIT` 6/6：重试递增、退避、达上限转 DEAD、死信不再领取、恢复后投递成功 |

## 2. 测试

```text
mvn clean verify → EXIT 0
单元 / 领域 / 架构：29
集成（真实 PostgreSQL）：27
合计：56
```

架构测试含 **6 项负向证明**，证明规则确实会拦截而非空跑。

## 3. Architecture Fitness

| 检查 | 结果 |
|---|---|
| 模块依赖方向、无环 | PASS |
| 库存不反向依赖采购/销售 | PASS（含负向证明） |
| Controller 不直接依赖 Mapper | PASS（含负向证明） |
| 领域层框架无关 | PASS（含负向证明） |
| 跨模块表访问 | PASS（含负向证明） |
| 建表注释规范 | PASS（含负向证明） |

## 4. Assumptions（分类与是否 Carry Forward）

| 项 | 分类 | Carry Forward |
|---|---|---|
| `RISK-VER-01` Spring Boot 3.3.x 维护状态 | `RISK_ACCEPTED`（用户 2026-09-21 知情接受） | 是 |
| 集成测试依赖本地 compose 数据库（非 Testcontainers） | `NON_BLOCKING`（真实 PG，满足 Gate G-06；不可达时显式失败不静默跳过） | 是 |
| `SPECIFIED_ORG/COMPANY` 暂按 `DEPT_AND_BELOW` 处理 | `DECISION_DEFERRED`（角色明细配置表在 P2） | 是 |
| `Q-09` 超收/负库存策略未答复 | `DECISION_DEFERRED`（P4 前需答复） | 是 |

## 5. Blocking Issues

**对进入 P2：无。** P2（主数据）不依赖 OIDC 接入。

**对生产发布：有一项。**

> `BLOCK-P1-01`：**生产环境当前没有任何认证路径。**
> 临时身份通道 `erp.security.dev-headers.enabled` 默认关闭且请求头可伪造，
> 不是认证；OIDC 令牌校验（auth-platform / Casdoor）尚未接入。
> 已实测 Casdoor 在 `http://127.0.0.1:8000` 可达且 OIDC discovery 正常，
> 接入前需在 Casdoor 注册 ERP 客户端（需用户决定客户端标识与回调地址）。
> **在该项关闭前，本系统不得对外暴露。**

## 6. Next Phase

```text
P2 Master Data（CAP-P05、CAP-G02、CAP-G04）
```

依赖 P1，已满足。并行可做：`BLOCK-P1-01` 的 OIDC 接入。
