# Gate Report — P5 Order to Ship

- Phase / slice：P5（沿用 ROADMAP 稳定阶段 ID）
- 结论：PASS_WITH_ASSUMPTIONS；五项阶段出口及双向追溯均 PASS。
- 验证时间：2026-09-22；执行者 Codex，接手 Claude 未提交实现。
- 验证命令：`mvn -q clean verify`，退出码 0。
- 结果：单元/架构 33 + PostgreSQL 集成 71 = 104；失败 0、错误 0、跳过 0。

## 可观察验收

| 条件 | 证据（OrderToShipE2EIT） | 结果 |
|---|---|---|
| 订单→审批→预占→两批出库→发运→签收，库存减少量与出库量一致，预占归零 | 分两批出库发运签收且双向追溯 | PASS |
| 库存不足预占失败，订单保留 APPROVED | 库存不足回滚且保持已审批、多行预占失败整体回滚 | PASS |
| 取消同时释放预占表及库存余额 | 取消同时释放预占余额和信用且不能再次预占 | PASS |
| 信用超限拒绝，经审批可下单 | 信用超限必须真实审批且只能使用一次 | PASS |
| 部分出库后可继续出库 | 分两批出库发运签收且双向追溯 | PASS |
| 单据图双向追溯 | 同上；Outbox 投递后订单查两张发货单，每张均可反查订单 | PASS |

## 失败与并发证据

12 个新增真实数据库测试还验证：草稿不能预占、审批实例不可串单、订单行不可串单、跨租户拒绝、负数/零/超精度/超发拒绝、发运前签收拒绝、并发预占取消无残留、并发出库不超发、并发信用占用不超限。
首轮窄验证发现 markReserved 的 SQL 引用了不存在的 newVersion 参数，修复后窄验证和全量回归通过。
数据库 PostgreSQL 16.15，使用现有本地实例的隔离 erp_it 库；Flyway 验证 13 个迁移，V80/V81 首次执行成功，重复启动校验通过。

## 范围与未验证项

- 销售实现是内部应用服务，没有新增 HTTP Controller；HTTP 认证授权、前端交互未验证。
- 客户授信上限来自可信内部调用方；信用放行绑定真实审批实例、订单内容摘要及数据库唯一使用约束。
- 订单状态变更留痕已复用内核机制；尚不能宣称全链路通用 AOP 审计完成。
- 业务日期暂用 Asia/Shanghai；完整租户时区链路未实现。
- 未改变已执行迁移；V81 增量约束兼容 V80。
- ShipmentPosted 保留既有事件名与关系字段；财务所需载荷由 P6 补齐。P6 尚未实现，不声称 MVP 闭环完成。
- 历史 BLOCK-P1-01 生产无 OIDC 认证路径仍开放，禁止对外暴露。
- 没有发现可恢复的 Runtime run_id。本报告是代码及本地测试证据，不伪造受治理 Runtime 已执行状态。

## 后续

按授权交付 P5；下一业务阶段为 P6 应收应付与核销。生产部署未执行。
