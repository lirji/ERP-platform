# P9 报表实施切片

依赖 P7 Gate PASS。实现库存余额、采购/销售累计统计、应收/应付账龄五类读模型。

- P9.1：权威模块通过 kernel 类型化导出端口提供租户内键集分页和变更指纹；reporting SQL 仅访问 rpt_*。
- P9.2：持久化重建任务、分批检查点、失败/中断恢复、候选快照原子发布；查询固定已发布快照和时间。
- P9.3：重建一致性、来源变更保护、租户/游标约束及 A04/A05 规模查询验证；提供重建 CLI 脚本及证据。

本期采用显式刷新快照报表，不承诺实时看板。重建前后对权威指纹复核；期间来源变更则不发布混合数据，保留上次快照并允许新建任务重试。所有批次有界提交，不持有全量扫描大事务。原业务接口不物理删除来源事实；人工修数后须重建。

状态：P9.1–P9.3 DONE；151 项回归、独立规模测试和 CLI 验证通过。
