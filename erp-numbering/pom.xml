<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.lrj.erp</groupId>
    <artifactId>erp-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>erp-numbering</artifactId>
  <name>erp-numbering</name>
  <!-- 编号中心：按 tenant/businessType/date/sequence 生成单据号，解决并发、唯一性与可读性。 -->
  <description>编号中心：按 tenant/businessType/date/sequence 生成单据号，解决并发、唯一性与可读性。</description>

  <!-- 拥有的表前缀：num_* （Data Ownership，见 MODULE_SERVICE_MAP.md） -->

  <dependencies>
    <dependency><groupId>com.lrj.erp</groupId><artifactId>erp-kernel</artifactId></dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <!-- 本模块拥有 num_* 表，需要持久层；SQL 只出现在 resources/mapper/*.xml -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
    </dependency>
  </dependencies>
</project>
