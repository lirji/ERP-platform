<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.erp.inventory.infrastructure.InventoryReportMapper">
    <select id="fingerprint" resultType="string">SELECT count(*)::text || ':' || coalesce(max(id),0)::text || ':' || coalesce(sum(version),0)::text FROM inv_balance WHERE tenant_id=#{tenantId}</select>
    <select id="page" resultType="com.lrj.erp.kernel.reporting.ReportSource$Row">SELECT id AS source_id,company_id,0::bigint AS partner_id,warehouse_id,sku_id,batch_no,
        NULL::date AS business_date,on_hand AS quantity,reserved,locked,in_transit,inventory_value AS amount,
        0::numeric AS reduction,0::numeric AS settled,0::numeric AS paid
        FROM inv_balance WHERE tenant_id=#{tenantId} AND id>#{afterId} ORDER BY id LIMIT #{limit}</select>
</mapper>
